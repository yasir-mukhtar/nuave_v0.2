# Fresh sessions: implement the approved recommendation-eligible path

**Portable checkpoint override:** on `codex/pause-checkpoint-2026-09-19`, read
[`docs/RESUME_DEVELOPMENT.md`](../../RESUME_DEVELOPMENT.md) first. This checkout
contains both the reviewed code and authoritative instructions. Treat the
original-machine absolute paths and “uncommitted” descriptions below as the
dated handoff history; use the checkpoint's Git state on the current laptop.
No new paid-run permission is carried by the backup. Publication cleanup made
the public content generic: the real test business's name, URL and private
evidence-directory names are withheld or replaced by clearly-labeled fictional
stand-ins; recorded evidence hashes are unchanged. `npm run verify` passed on
the final content before publication; confirm the remote branch tip matches
local `HEAD` before relying on it.

Updated 2026-09-19. The founder approved the combined plan and requested fresh
orchestrator and worker sessions. [Spec 009](../../../specs/009-recommendation-eligible-audit/SPEC.md)
is the single execution contract. The old opinion-only and fixed-slot handoffs
are superseded. The winning-source checkpoint remains quality evidence.

**Latest progress record:** [NUAVE-LIVE-REPORT-2026-09-19](../2026-09-19-live-direct-ten-report/CHECKPOINT.md)
preserves the completed real run and the founder's progress/pace feedback.
That checkpoint is a dated snapshot. Resume from the current orchestrator
handoff below; older pending actions do not override the latest status.

**Current continuation:** the three code corrections are accepted. The
orchestrator verified the fresh Chrome journey, edited-question continuity,
Back/reload preservation and normal JSON download. The founder has now reviewed
the PDF and reaffirmed testing the complete workflow before improving its
format. No further worker coding round is needed on current evidence. Report
usefulness/format work remains deferred.

## Current orchestrator handoff — 2026-09-19

The founder wants another agent to take over the orchestrator role at this
milestone. This is a prepared handoff; no agent has been created or dispatched.
Continue the existing work, not a fresh implementation or another broad review.

```text
Take over as Nuave's orchestrator. Your first bounded deliverable is one
accurate, reviewable proposal for a complete real end-to-end local audit test.
Explain it to the founder in plain language, including expected cost, retries,
what they will review, and the precise paid-call authorization needed.
Do not make the calls before that authorization.

Canonical project documents: /Users/yasir/nuave_v0.2.
Implementation: /Users/yasir/nuave-worktrees/spec009-block-a,
branch codex/spec-009-block-a-direct-ten, base/HEAD
687f340343aa5be547d03e7d6fe23a9f5262a2df with uncommitted implementation.
Both directories contain existing work. Preserve it. Starting another checkout
without carrying these changes would lose the implementation under review.

Read only:
1. Canonical AGENTS.md, README.md and the current sections of docs/NOW.md.
2. docs/WORKFLOW.md for role boundaries and founder-relayed handoffs.
3. specs/009-recommendation-eligible-audit/SPEC.md, especially its approved
   September 19 continuous-experience amendment.
4. The two latest sections of that package's VERIFICATION.md: founder PDF
   review and continuous-flow correction review; then DEFERRED_NOTES.md.
5. This file's “Current step — complete real end-to-end test” section.
Inspect directly connected code only when needed to make the proposal accurate.
Do not read archive or execute the older handoffs below.

Established:
- The founder accepted the direct-ten GLM question method and ten real
  retained-business questions. No old 6/4 split, fixed roles or forced
  question diversity.
- Those saved questions completed a real audit: ten grounded answers and one
  report, all eleven requests successful, no retries. Evidence is retained.
- The ordinary new-business intake is now connected through facts, editable
  questions, approval, progress, report and downloads. That continuous flow
  was tested with clearly labeled synthetic responses, not paid providers.
- Completed reports survive Back/continue and reload without another run.
  Captured-answer recovery, preparation mode selection and retry accounting
  corrections are accepted. JSON keeps exact questions and full answers.
- Worker reports final npm run verify passing: 1,241 unit tests, 102 browser
  tests and both builds. Orchestrator independently passed 31 focused tests
  in the last correction review and exercised the normal Chrome journey.
- Founder reports having viewed the PDF. Offline AC-10 is accepted for moving
  to a real test. Do not reopen PDF proof or report redesign before that test.
  Report usefulness AC-08 remains deferred; Spec 009 is not globally Verified.

What remains unproven is the entire fresh-session journey with real providers
in every stage. The next proposal must include real source preparation,
one fresh GLM generation, explicit fact/question approval, ten independent
grounded observations and the current report/downloads. The retained
business's public context is already supplied; preserve the earlier accepted
pack as evidence,
not a hidden replacement for the current session. Expected successful path:
one extraction + one GLM generation + ten observations + one report.

Generation stays GLM 5.3 Flash via Cheaper Inference. Audit/report stays
OpenCode Go / gpt-5.6-luna / low. Verify actual limits before presenting cost:
one report workflow has an initial synthesis and at most one language repair;
the general report-stage allowance of three is a separate retry limit.
SDK transport retries may add requests and billing. The USD 5 reservation
guard is not a guaranteed supplier billing cap. Last recorded audit carryover
is USD 0.60020316. Earlier paid authorizations are consumed; the previous
worker's proposed live test incorrectly left GLM generation synthetic.

Keep report format, excerpts, conclusions and recommendation improvements
deferred. The founder specifically wants exact questions and full AI answers
accessible in a later report. Save each test's evidence JSON beyond the browser
session so presentation and analysis can reuse it. Record unrelated issues
in the existing DEFERRED_NOTES.md without pursuing them.

The founder relays worker tasks. Do not implement application changes or
dispatch another worker merely to take over. No commit, push, merge,
deployment, paid run, payment or hosted-delivery work is authorized here.
Only fix a specific flow blocker if one is actually demonstrated later.

Private real evidence is in the canonical checkout's gitignored dated
`.secrets/spec009-…-2026-09-18/` evidence directories (the directory names are
withheld from the public repository). Do not copy raw answers or credentials
into public docs. Offline review artifacts/logs are at
/tmp/nuave-continuous-final-review/. Port 3031 was confirmed loopback-only,
with live flags off and provider keys blank, at handoff preparation. Recheck
the process if you need the preview; never infer authorization from an env flag.

Return a short takeover acknowledgement and the one concrete next test
proposal. Do not restart planning, repeat the full verification suite without
a new reason, or reopen accepted question-method decisions.
```

## Current step — complete real end-to-end test

Offline flow acceptance combines the independent browser/JSON/recovery checks
with the founder-reported PDF review. The next bounded action is preparing one
paid-test proposal for the ordinary local journey, with all real stages:

business/source → prepare and confirm facts → generate ten GLM questions →
edit/explicitly approve → ten independent grounded answers → report → PDF/JSON.

Expected successful path: one extraction, one GLM generation, ten observations
and one report synthesis, plus source fetching. Human fact/question review
remains part of this path. This is a scope description, not authorization or a
hard call/billing cap; confirm actual retries, accounting and allowance before
requesting the paid run. The older worker proposal omits real GLM generation
and must not be relayed unchanged. Earlier live permissions are consumed.

Save the exact approved questions, full captured answers, sources, model/time
and confirmed brief with the run. The existing JSON exporter preserves these
fields. Later layout and full-answer presentation should reuse this evidence
without repeating observations. Better interpretation may need analysis of the
same answers; do not label all report-quality work as formatting.

The founder's full-question/full-answer preference is recorded in the existing
deferred note. Do not implement it before the real workflow test. Payment,
hosted delivery, publication and report-quality acceptance remain outside this
local test. No new worker task or application change is requested now.

## Current step — founder PDF check

**Previous step — founder now reports having seen the PDF.** Retained below
as the manual handoff record; it no longer blocks real-test preparation.

The founder chose “I’ll save the PDF myself.” Use the existing Chrome tab
“Uji coba intake | Nuave” in the “🧾 Nuave export check” group at
`http://127.0.0.1:3031/audit/new-intake?glm=1`. It holds the completed synthetic
Batik Laras report; the explicitly offline loopback preview remains running.

1. Select “Cetak / simpan PDF”, choose “Save as PDF”, and save the file.
2. Open that PDF and check the business name, all ten question details,
   synthetic labels, and the edited first question: “Rekomendasi penjahit
   batik tulis untuk seragam kantor di seluruh Indonesia apa ya?” The output
   should contain the report, not the question editor.
3. Record the file path and outcome in the canonical verification record.
   Do not rerun observations or synthesize another report for this check.

The normal JSON download already passed and is retained at
`/tmp/nuave-continuous-final-review/chrome-evidence.json`. The worker's
`page.pdf()` artifact proves print content only; it does not close this step.
AC-10 remains pending solely for the normal PDF save/artifact check.
[Independent correction review](../../../specs/009-recommendation-eligible-audit/VERIFICATION.md#continuous-flow-correction-review--2026-09-19)
records the accepted fixes and the browser-control limitation.

After PDF confirmation, prepare one real-flow approval proposal that includes
fresh GLM generation, human approval, preparation, ten observations and report.
The worker's latest proposal leaves question generation synthetic; do not
relay it unchanged as a complete live test. Previous paid permissions are
consumed. Report quality, payment, hosted delivery and publication stay outside
this step.

## Current correction — continuous-flow review

**Historical handoff — returned and reviewed. Do not relay again.** Three code
items are accepted; the founder PDF check above owns the remaining evidence.
Source references and
reproduction are in the canonical
[verification record](../../../specs/009-recommendation-eligible-audit/VERIFICATION.md#continuous-flow-return-review--2026-09-19).

```text
Continue /Users/yasir/nuave-worktrees/spec009-block-a on the same branch.
Preserve all dirty work. Your main continuous-flow wiring is retained; do not
restart it. Canonical docs are /Users/yasir/nuave_v0.2. Read only current NOW,
Spec 009's September 19 amendment, and VERIFICATION's “Continuous-flow return
review — 2026-09-19”, then the directly affected code.

Finish ONLY this consolidated list:

1. Preserve completed results and recover captured answers.
   Reproduction: /audit/new-intake?fixture=GLM&glm=1 -> complete facts/questions
   and synthetic audit -> report -> Kembali ke pertanyaan -> unchanged Mulai
   audit. Orchestrator saw “running”, 10/10 complete, no report; reload showed
   interrupted/report unavailable, and Continue returned 422 because synthetic
   observations failed protected-model validation. Evidence is in
   /tmp/nuave-continuous-review/back-reentry.json and server.log.
   Reopen the matching saved report without execute/report calls or clearing
   it. Correct interrupted/partial-capture recovery for the explicit offline
   substitute path without relaxing protected live evidence checks. Cover
   return-and-continue (not only Back), and resume with saved observations
   (not only zero-progress interruption). Keep original/edited text binding.

2. Finish entered-business preparation mode selection.
   prepareEnteredBusinessFixture always adds substitute=local and
   local_mode: synthetic. The newly entered source can never reach real
   preparation through this flow. Select offline versus authorized live
   preparation on the server with explicit provenance, reusing existing
   boundaries/guards. Default offline; no live call or credential read now.
   Prove the live branch with mocked providers, and carry preparation usage
   into the current session budget. Do not remove safety gates, silently enable
   extraction, or present fixture facts as source-derived facts.

3. Preserve report retry accounting and prevent duplicate retries.
   callReport currently sends budget.calls = working.runCalls only, losing
   earlier reportCalls; reportCallAttempts increments but is unused by the
   retry limit. Failures with no telemetry leave the retry allowance unchanged.
   Carry/deduplicate all known stage usage on retry, retain a bounded retry
   counter when telemetry is unavailable, and guard the direct retry action
   before submitting so repeated clicks cannot overlap. Keep existing policy
   and do not claim the client ledger is a hard provider-billing cap. Add the
   focused request-history/empty-telemetry/double-retry regression.

4. Demonstrate normal PDF and JSON output for a new-business session.
   The existing test replaces window.print with a counter, then calls page.pdf;
   the entered-business test exports JSON only. Return actual opened PDF/JSON
   produced by the normal controls in a supported browser for that same new
   business and edited question. Retain artifact paths; inspect names, wording
   and report-only content. Reuse the existing print path; no layout redesign.
   If the automation browser cannot operate its native save dialog, state that
   specific limit; don't substitute a separately generated PDF and mark passed.

Keep report usefulness, excerpt selection, recommendation classification and
format improvements deferred. Do not reopen GLM/method choices or other notes.
Use an explicitly loopback-bound preview for review; a localhost URL alone is
not evidence that the process listens only on loopback. No broad test-config
cleanup is requested.

Re-run relevant regressions and the canonical npm run verify after final edits.
Review the complete diff. Return one concise VERIFICATION update: separate
continuation time, exact preview launch command/URL and mode, evidence for each
item, retained PDF/JSON paths and checks, plus any exact remaining blocker.
No paid call, credential read, commit, push, merge, deploy or agent dispatch.
The previous single-run permissions remain consumed. Do not propose publishing
or a new live run before this bounded functional correction is reviewed.
```

<a id="current-worker-task--continuous-audit-flow"></a>

## Previous worker task — continuous audit flow

Returned on September 19. Preserved as the original approved task; use the
current correction above instead of relaying this whole task again.

```text
Continue Nuave Spec 009 in /Users/yasir/nuave-worktrees/spec009-block-a on
codex/spec-009-block-a-direct-ten. Preserve all dirty work and the retained
real-run evidence. Canonical documents are in /Users/yasir/nuave_v0.2; read
those versions rather than stale worktree copies.

The founder's new instruction is:
“The report isnt that useful now. I can improve the format later. But now
what we need to rush to complete is the end-to-end audit experience, despite
the report is not match our standard yet.”

Your single deliverable is one continuous local browser flow:
enter business/source -> prepare/review/confirm facts -> generate ten GLM
questions -> edit and explicitly approve -> run those exact ten questions
with visible progress -> read the existing report -> download PDF and JSON.
Keep report usefulness/format improvements deferred. Do not stop this task
because the current report is under the desired quality bar.

Read the canonical AGENTS.md, NOW.md and Spec 009, especially its approved
“2026-09-19 amendment — finish the continuous audit experience”. Read only
connected implementation dependencies after that. Reuse the accepted question
method unchanged; no need to re-review historical prompt experiments.

Confirmed gaps to close:
- IntakeJourney's reading phase uses local fixture preparation rather than
  the entered business's existing identity/extraction boundaries.
- Its final action creates LocalStartHandoff and ends on a handoff screen.
- Its demo link opens /audit/local-report, which loads the separately saved
  accepted retained pack, rather than the just-approved current-session pack.
- Real-run browser download/print has not been verified; the preserved live
  PDF was a separately authored review copy, not the product print output.

Connect these using existing components/routes, not a second audit workflow.
Likely touchpoints: src/lib/intake/IntakeJourney.tsx and its session adapters;
src/app/audit/new-intake; existing identity/extract/glm-questions/run/report
boundaries; the brief/pack projection in local-direct-ten-audit.ts; existing
AuditRunStep, ReportView and customer-evidence-export. Follow direct dependencies
only. Keep local-report as retained regression evidence if useful; it cannot
be the normal fresh-session path or a source of another business's facts.

Use the existing local enablement and explicit live permissions. Offline
substitutes must be labeled and injected at the real boundaries. No live
provider calls, key reads, new authorizations, regenerated accepted questions,
manual evidence/receipt editing or silent fixture replacement. The earlier
single-run allowances are consumed. Carry existing usage correctly; newest
recorded audit ledger is USD 0.60020316, not the older 0.4357.

Back/reload preserve confirmed inputs, edits and completed results without
restarting a paid stage. Show interrupted/failed work honestly and keep
captured progress. Prevent double starts. Preserve exact approval binding,
independent brand-free observation requests, ten-evaluable delivery floor,
truthful counts, evidence provenance and existing budget controls. A PDF
retry/download must use the same report, not run another audit. No new durable
job, account, payment or hosted-report architecture is needed for this task.
If an optional comparator or misleading demo label directly blocks this fresh
flow, make only that narrow correction; record all other issues in the existing
DEFERRED_NOTES.md and continue.

Prove one new-business journey in the browser with an edited/approved question
and actual opened PDF/JSON from its normal controls. Use labeled offline
provider substitutes at the actual routes. Include focused checks for stale
approval, failed/interrupted stages and no duplicate/incidental calls. Finish
npm run verify and inspect the complete diff. Do not call the task complete
from a passing component test or a separate saved-pack demo alone.

Work directly toward the runnable flow; no new planning round or redesign.
Record active time for this continuation separately from prior budgets. Return
one concise result in Spec 009 VERIFICATION.md: URL/mode, exact click path,
changed files, evidence of same-session input/text continuity, artifact paths,
checks and outcomes, and any remaining blocker. If incomplete, preserve the
working state and name the exact missing connection and completion estimate.
Do not silently open another correction round. No commit, push, merge, deploy,
customer delivery or agent dispatch. I will relay your result to the orchestrator.
```

## Worker continuation — approved 90-minute completion extension

Completed and returned. Preserved as the scope of the approved extension;
do not relay or restart it again.

```text
Continue in /Users/yasir/nuave-worktrees/spec009-block-a on the existing
codex/spec-009-block-a-direct-ten branch. Preserve all dirty work.
Canonical documents: /Users/yasir/nuave_v0.2.

The founder explicitly approved the additional 90 minutes and said:
“Yes of course, allowed. But be focus and fast, do not go chasing other goal
or issues other than what already determined. Store issues found along the
way (if any) in an .md note that we can revisit later.”

Start a fresh extension clock now: 90 additional ACTIVE minutes TOTAL,
including investigation, edits, tests, final npm run verify and return.
Aim for 60 minutes implementation and 30 verification. Keep prior time intact
and report extension time separately. Do not spend time reconstructing old
timing or rereading historical planning. Use the current canonical NOW.md,
Spec 009 R-06/R-07/R-08 and the VERIFICATION.md correction-return review as
needed. The exact scope is already settled.

Finish ONLY these four outstanding items:

1. Connect the accepted-pack browser flow to the existing live audit/run and
   report boundaries, with explicit local mode and authorization controls.
   The current local-report page only posts to the synthetic-only local-audit
   route; granting live permission alone cannot make it execute the real run.
   Carry the same approved questions, bound business context and run results
   through the browser flow. Reuse the finished review UI, lock and provider
   code. Prove the connection offline using provider substitutes at those actual
   boundaries. Retain zero sends on mount/Back/reload. Do not make a paid call.

2. Make the direct-ten headline recommendation count use all ten evaluable
   answers, separately from appearance. Completed answers with not_assessed
   judgments must not silently shrink that denominator. The reproduced case
   is ten completed answers, one endorsement, nine not_assessed judgments:
   current normalization/validation/build accepts and reports 1/1, not 1/10.
   The evidence is /tmp/nuave-blockb-review/denominator-counterexample.json.
   Add the focused regression for this case; preserve uncertainty honestly,
   the ten-evaluable delivery floor, and historical-method behavior.

3. Print/download the report with its relevant disclosure, excluding the
   question editor and run/reset controls. The returned 11-page PDF prints
   the editor on pages 1–2 before the report. Inspect the resulting actual
   PDF and JSON from the browser flow. No layout redesign or general polish.

4. Prepare an accurate live authorization proposal tied to that connected
   path. State expected use separately from enforced limits; include logical
   calls versus SDK transport retries, actual timeout, advisory search caps,
   report retry behavior and spend carried from run to report. The installed
   SDK defaults are two retries and a ten-minute timeout unless overridden.
   Next-call reservation is not cumulative billing and does not imply a
   13-call maximum; one requested search is not a guaranteed cap. Preserve
   existing budget controls, implement only the minimal enforcement needed
   for the agreed bounded local run, and describe remaining accounting limits
   accurately. No new generic budget system or provider/model research project.

The accepted ten questions remain unchanged. Reuse completed retained-review,
persistence, Indonesian detail copy, local-only guards and export provenance.

For ANY unrelated issue noticed along the way: append a brief observed fact,
location and possible impact to specs/009-recommendation-eligible-audit/
DEFERRED_NOTES.md; then continue. Use the canonical note as the starting copy
if your worktree lacks it, preserving existing entries. Do not investigate,
fix, test or promote unrelated notes to acceptance gates. A direct blocker of
one of the four items stays within that item and clock; explain why it is needed.

After final source edits, run npm run verify and inspect the complete diff.
Do not broaden tests after the relevant checks pass unless a new failure
requires it. The preview must bind to 127.0.0.1; the review server at 3031 was
started with provider keys blanked. Resolve any port clash before verification.

Return a concise VERIFICATION.md update: completed/remaining status for these
four items, exact extension time, final full-gate result, working local URL,
offline browser evidence, actual PDF/JSON paths, and the concrete live proposal.
List deferred issues separately without asking to solve them now. Stop at the
90-minute deadline if incomplete and report the exact remaining blocker.

No new agents, question regeneration, paid provider calls, commit, push, merge,
deployment, public real-business evidence or broader refactor. Do not claim
completion until the four existing requirements and their checks actually pass.
Return to the founder; the orchestrator reviews only this agreed scope.
```

Copy the first prompt into a fresh orchestrator session. It will guide the
founder through the two blocks. Copy the second into a fresh worker session
when the orchestrator releases Block A. The founder relays returns; agents
do not create sessions or contact each other automatically.

## Fresh orchestrator prompt

```text
Continue as my Nuave orchestrator for the approved recommendation-eligible
implementation. I will relay your handoffs to a fresh worker and return its
results. Do not duplicate implementation or dispatch agents.

Canonical repository: /Users/yasir/nuave_v0.2
Reusable worker code: /Users/yasir/nuave-worktrees/glm-prototype

Read, in order:
1. AGENTS.md, README.md, docs/NOW.md, and docs/WORKFLOW.md.
2. specs/009-recommendation-eligible-audit/SPEC.md in full.
3. docs/references/WINNING_QUESTION_GENERATION_PROMPT_2026-09-07.md in full.
4. docs/DECISION_LOG.md entry “2026-09-18 — approve the bounded
   recommendation-eligible delivery”.
Use the worker opinion only if a code-dependency claim needs clarification;
its twelve/select-ten recommendation was not adopted.

The settled flow is one brief -> ten directly generated unnamed questions ->
human editing and explicit approval -> ten independent grounded answers ->
downloadable report. No fixed purposes, 6/4 split, selection pool, mandatory
comparisons, diversity quota or punctuation-count gate. GLM-5.3 Flash remains
the writer; the grounded audit provider remains OpenCode Go/GPT-5.6 Luna.
Do not reopen these decisions or ask for another general plan approval.

Your first block is 15 active minutes. Check current checkout/worktree status,
preserve all uncommitted work, confirm the approved documents are accessible,
and ask me which business/public context we should use while preparing the
worker handoff. The old local server was last reported synthetic; do not
assume it is a live product. Give me the ready Block A prompt from this file.
Do not spend the session re-reading days of historical reviews.

Worker A is capped at 150 active minutes including checks. Review its return
within 15 minutes: inspect the exact source/request diff, use the local UI,
verify the new-method pack and existing send/evidence protections. Then show
me the concrete request, model, call count and cost estimate and ask for
one new generation authorization. All prior allowances are consumed.

After that authorized call, show the actual ten questions and their business
context before your judgment. Ask me to edit, approve as good enough, or flag
concrete problems. Record my exact verdict and wording. Only release Worker B
after I accept the real questions; direct-ten still includes this quality gate.

Worker B is capped at 210 active minutes including full npm run verify. Review
within 15 minutes, focusing on end-to-end behavior, no brand/context leakage
at the answering-model boundary, truthful report counts and an actual working
download. Then prepare a separately bounded live audit authorization covering
the ten approved observations, report synthesis, any other calls, budget and
retry/stop rules. Once I authorize it, let me use the real flow and judge the
report's usefulness. No provider call is authorized by this session prompt.

At a time-box deadline, return working progress and the exact remaining
blocker. At most one consolidated correction list per block; do not extend
budgets, create new gates or silently relax criteria. Ask me about an extension
only with concrete remaining work. Cosmetic polish must not restart a cycle.

Use the approved spec's acceptance criteria. Update only necessary status,
decision and verification records; do not produce another planning framework.
Mark incomplete/live-pending honestly. No paid calls, credential reads,
commits, pushes, merges or deployment without the applicable authorization.
Return the runnable local entry, what is real versus synthetic, and the next
smallest action. Keep the founder involved at the question and report checks.
```

## Fresh worker prompt — Block A only

```text
Implement Block A of Nuave Spec 009: confirmed business brief -> ten natural
unnamed GLM questions -> editable and approvable local pack. Offline work only.
Hard budget: 150 active minutes total (aim for 120 implementation + 30 checks).
Stop at the deadline with the working state; do not start Block B yourself.

Canonical documents: /Users/yasir/nuave_v0.2
Reusable uncommitted implementation: /Users/yasir/nuave-worktrees/glm-prototype
Read AGENTS.md, README.md, docs/NOW.md, then the complete
specs/009-recommendation-eligible-audit/SPEC.md and
docs/references/WINNING_QUESTION_GENERATION_PROMPT_2026-09-07.md.
Read only the additional sections/code named by Spec 009 for your slice.

First state objective, intended files, timing and any actual blocker. Read
current Git status in both checkouts. Fetch current origin/main and create a
dedicated codex/ branch/worktree from it; preserve both existing trees. Carry
forward the approved Spec 009/routing changes and only needed reviewed GLM/UI
work, explicitly listing what you reuse. Do not blindly copy the old matrix
writer or reset its evidence directory. Record the actual base and work path.

Implement the approved direct-ten flow, not twelve/select-ten. Preserve the
winning instruction with a concise exact adaptation diff, the no-quota
amendment and one minimized brief. No fixed roles, named questions, required
competitor, per-slot context or punctuation proxy. Human quality approval stays.

Reuse the existing transport, key isolation, attempt binding, response capture,
original/edit persistence and honest errors. Introduce only the minimum
versioned pack boundary needed for ten purpose-free texts. Remove old grouping
and purpose restrictions from this method's review path. Do not attach fake
legacy metadata. Preserve originals and explicit human corrections. No silent
fallback, trimming into a different question, fabricated completion or
incidental provider sends. Keep the old method functional for historical data.

Show a runnable local UI with explicitly synthetic or retained-response data.
Demonstrate edit, Back, reload, invalidated approval and successful local pack
approval. Do not imply this proves new live naturalness. Use the supplied
business context if available; otherwise a clearly fictional fixture is fine
for offline plumbing. The founder will confirm live context before freezing.

Prepare, but do not send, the exact new GLM request for the founder checkpoint.
Preserve the accepted source body; report the full request and adaptation diff,
model, output limit, 180-second wait, zero client retries, hashes and estimated
cost. Never edit the prior consumed request/marker or read credential values.
If real input is pending, mark the live request not yet ready rather than
claiming a fixture is authorized.

Run proportionate focused tests, including preserved B1–B3 protections, honest
extraction, complete founder context-plus-request examples, no calls on refresh,
and original/edit persistence. Full npm run verify is the Block B delivery gate;
Block A must not be labelled branch-ready or fully verified.

In specs/009-recommendation-eligible-audit/VERIFICATION.md record a concise
Block A return: time used, branch/base/worktree, files changed/reused, tests,
source diff, local URL/mode, request readiness, known failures and the exact
next founder test. Return it to me for the orchestrator; do not dispatch agents.
Stop after this block. No paid calls, automatic retries, credential reads,
commit, push, merge, deployment, model comparison or broad refactor.
```

## Worker continuation — Block B, released 2026-09-18

The founder accepted the real ten-question pack. This is the ready continuation
for the same worker, relayed by the founder. No agent is dispatched automatically.

```text
Continue with offline Block B of the approved Nuave Spec 009. The real-question
checkpoint has passed. Exact founder verdict: “Yes this is good enough, beyond
what we achieved in the 6/4 formula. Move forward.” All ten retained-business
questions were accepted unchanged. Do not regenerate or reopen question quality.

Canonical documents: /Users/yasir/nuave_v0.2
Implementation worktree: /Users/yasir/nuave-worktrees/spec009-block-a
Branch: codex/spec-009-block-a-direct-ten
Recorded origin/main base: 687f340343aa5be547d03e7d6fe23a9f5262a2df
Preserve all uncommitted work and the original prototype.

Accepted evidence directory:
/Users/yasir/nuave_v0.2/.secrets/spec009-<private>-2026-09-18/
(the exact directory name is withheld from the public repository)
Read ACCEPTANCE.md and ACCEPTANCE.json, then accepted-local-pack.json,
frozen-intake.json and QUESTIONS.md. This is retained real GLM output,
not synthetic questions. generation-result.json and the raw response remain
available there if needed for provenance/billing. Do not copy raw provider
metadata or this real-business evidence into public fixtures or Git.

Verify these bindings before coding:
- method: nuave-glm-direct-ten-v1
- accepted-local-pack.json SHA-256:
  0190ccc0ccc96178f655ebaecb8d99bc845d7eb985d99d42f7f230cc3ff3cda7
- ordered text SHA-256 (UTF-8 JSON.stringify of the question string array):
  0034f2271ae6abef0848cea349015f381c999b71429448992fe95d07fd87255e
The acceptance receipt also binds the business input and generation request.
If a record is missing or mismatched, report the exact discrepancy; do not
reconstruct questions, alter frozen artifacts or invent acceptance.

Approval was in the orchestrator conversation, not the local UI. The native
pack's draft/review-status fields were left as emitted by your builder.
Carry the recorded human approval and exact accepted texts/facts into the
real review/lock/run path. Do not invent a prior UI approval event. Changes
must invalidate stale approval. No second naturalness gate is required.

Hard budget: 210 active minutes total (aim for 150 implementation + 60 full
verification). Continue on the recorded current-main-based implementation
branch. Read canonical AGENTS.md and docs/NOW.md, Spec 009 in full, your Block A
VERIFICATION.md and the canonical Spec 009 VERIFICATION.md (the latter records
the live generation and founder acceptance). Carry forward necessary canonical
status updates without overwriting your implementation or Block A return.
Read only the directly required Product/Audit sections and connected code.
State objective, files, timing and real blockers before editing.

One consolidated Block A carryover list; close within its remaining original
budget (you reported ~135/150 minutes), with no silent extension:
1. The returned port 3027 still serves glm-prototype, not spec009-block-a.
   Provide and verify a working URL from your implementation worktree. Show
   retained-response review, original/edit persistence, Back/reload and approval
   invalidation with zero new provider sends. Label the response as retained.
2. The real provider receipt has settled billed_cost_usd as the string
   "0.000496"; your number-only parser reports null. Parse valid finite
   nonnegative numeric strings as well as numbers, reject invalid values,
   retain the receipt, and add focused coverage. Do not rewrite raw captures.
Record time separately; Block B remains capped at 210 active minutes including
checks. Return concrete blockers/estimates at either deadline rather than
extending time or opening another correction cycle.

Connect that same versioned approved pack to the existing independent grounded
observation engine, web report, PDF and evidence download. Remove matrix
assumptions for this method at lock/run/report boundaries. Preserve historical
contracts explicitly. IDs do not assign purpose. No new taxonomy or model
intent labels as report authority.

Prove at the actual outgoing answering-request boundary that the exact approved
question is sent independently, without the target identity, business brief,
competitor list or other answers. Target identity may remain in server matching
and later report synthesis. Preserve model/search/response provenance and all
applicable privacy and cost controls.

Use offline fixtures through the real execution/report code to demonstrate the
complete local journey. Keep fixture labels honest. Inspect the report and
open the generated PDF/evidence download. Mention and recommendation are
distinct; untested is not zero; ten evaluable observations remain required.
Show no legacy position-derived dimensions for the new method.

Run focused regression checks, then npm run verify; inspect the complete diff.
Update VERIFICATION.md with exact state, evidence, remaining criteria and time
used. Do not claim completion if tests or download inspection are unfinished.
Prepare a concrete live-run authorization proposal listing all necessary
observation/report/source calls, costs and retry/stop limits; do not execute it.
The prior generation approval does not authorize the audit/report calls.

Stop and return the local URL/mode, changed files, verification result, actual
download location and next founder test. At deadline, return working progress
and a concrete remaining blocker. No silent budget extension, new model calls,
commit, push, merge or deployment. One unresolved cosmetic issue is not a new
implementation project.
```

## Worker return — one Block B correction list, 2026-09-18

This is the single consolidated Block B return, already relayed and returned.
It is preserved as the scope of that round, not a new continuation authorization.
The full evidence and criterion status are in the canonical
[verification record](../../../specs/009-recommendation-eligible-audit/VERIFICATION.md#orchestrator-block-b-review--2026-09-18).

```text
Continue the existing Spec 009 worktree/branch. Block B is useful progress but
not complete. Preserve the direct-ten boundary work and the accepted
retained-business questions exactly; do not regenerate or reopen naturalness.

Canonical repo: /Users/yasir/nuave_v0.2
Implementation: /Users/yasir/nuave-worktrees/spec009-block-a
Branch: codex/spec-009-block-a-direct-ten
Read the canonical Spec 009 VERIFICATION.md's “Orchestrator Block B review”
and this single correction list. Reuse the released handoff's private evidence
paths, hashes, approval and approved specification. Preserve all dirty work.

Budget: you reported ~140/210 active Block B minutes used. At most ~70 remain,
including investigation, fixes, full npm run verify and the return. Block A's
~15 remaining minutes were reported spent; do not reset either budget. If the
remaining work cannot fit, stop at the deadline with exact blockers and an
estimate for founder extension. This is the one correction list, not a new
project or automatic extension. Work on the following existing requirements:

1. Finish the smallest same-pack local product path. The intake handoff currently
   links to a fixed synthetic demo posting {}, and the retained editable review
   was descoped. Carry the accepted retained pack and receipt-bound input through
   review/lock/run/report. Reuse the existing review UI where practical; preserve
   originals and edits across Back/reload, invalidate stale approval on changes,
   and show that reload/Back make zero provider sends. Existing unchanged texts
   retain the founder's recorded approval; do not demand another naturalness
   decision. Load real context from restricted bound input, not hardcoded
   business facts in source/public fixtures. Reject missing/mismatched/unknown method or
   context before calls. Make the real execution path reachable from this local
   flow after separate authorization; demonstrate it offline with explicitly
   synthetic provider substitutes. Do not replace it with script-only API calls.

2. Correct and prove the new-method report/download. Recommendation currently
   filters to brand-appeared records (contracts.ts around line 1234); the captured
   ten-completed demo shows 0/10 appearance but 0 assessed recommendations.
   For direct-ten, report appearance and explicit recommendations separately
   across ten evaluable answers as R-07 requires. Mere mention is not endorsement;
   a failed answer is not absence. Preserve historical denominators. Use a small
   offline mixed-answer regression (explicit endorsement, mention-only, absence,
   failed answer) and retain the ten-evaluable delivery gate. Keep named recognition
   untested or omitted. Carry the explicit question-method/version into the
   downloadable evidence instead of dropping the local response's provenance.
   Keep delivered findings/evidence notes Indonesian; no broad copy polish.
   Demonstrate the actual browser flow: the reviewer's page showed only the run
   button after clicks/reload, although the synthetic API returned 200. Save and
   open the actual PDF and JSON from that flow; inspect counts, text, labels and
   layout. Calling window.print or citing an old fixture test is not PDF inspection.

3. Enforce founder-local scope at the new live boundaries. /api/audit/run and
   /api/audit/report currently accept direct-ten without the local/non-production
   guard used by the demo. Fail closed outside founder-local mode before provider
   work; preserve the existing canonical route. Verify disabled/production cases
   make zero calls. Bind the dev preview to 127.0.0.1. Keep synthetic and live modes
   explicit and preserve all existing no-extra-send, privacy and budget controls.

4. Replace the unsupported live proposal. “<=11 calls, $0.006–0.01” conflicts
   with the current three-attempt observation policy, possible report-language
   retry and $0.01-per-search accounting. Prepare a concrete proposal tied to
   the actual product path, stating expected and maximum observation/report/
   source calls, search/token pricing basis, cumulative carryover, enforced budget,
   timeout/retry/stop behavior and what is excluded. Do not call an estimate an
   upper bound. Reuse existing limits or explicitly enforce a narrower proposed
   limit; the proposal and implementation must agree. No live call is authorized.

After the final source edit, run npm run verify and inspect the complete diff.
Your previous full pass preceded the last edits; focused rechecks do not replace
this gate. Update your VERIFICATION.md with time used, exact tests/final tree,
usable local URL, browser behavior, private downloaded artifact paths, criterion
status and the corrected proposal. The orchestrator independently passed 93
boundary/report tests and the zero-provider synthetic API, not the full journey.

Stop and return to the founder. No new agents, provider calls, commit, push,
merge, deployment, public real-business fixtures or broad refactor. If incomplete,
say so; do not silently descope acceptance criteria or claim Block B complete.
```

## State to preserve

- Latest Laundry Ceria live generation allowance was consumed; response
  HTTP 200/stop, billed USD 0.000782, followed by `validation_failed`.
  No approved pack/audit/report resulted. Its evidence is under the original
  worker worktree's `.local-evidence/glm/`; never reset the consumed marker.
- That worker changed the frozen intake after a failed submit while retaining
  the outbound request hash. The deviation remains recorded; never repeat it
  to get a new authorized send through.
- The source body SHA-256 remains
  `652cfeda5cb6b11fa08d33f80325854738fdaacd8f0f59b3533ae0ad0cf71d0a`.
- The historical invoice capture was “Mostly—some questions need edits”. Keep
  the founder's exact wording and source quality reference; do not claim every
  prior question passed.
- These revised documents are local and uncommitted until the founder asks
  to commit/sync them. Fresh sessions on another device need them transferred
  explicitly; do not assume origin/main contains this contract or prototype.
