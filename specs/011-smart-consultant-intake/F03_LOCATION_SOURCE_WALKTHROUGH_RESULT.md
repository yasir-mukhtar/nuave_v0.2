# F-03 location-source live preparation result

2026-09-24. **One authorized Periksa completed; allowance consumed.**
The summary now contains the required reach proposal and confirmation is enabled.
Confirmation was not clicked. The founder accepted the reach and prepared
summary on 2026-09-24. Formal Spec 011 acceptance closeout remains pending.

## Authority and product

The founder replied **“Approved”** to the
[preparation-only handoff](./F03_LOCATION_SOURCE_WALKTHROUGH_PROMPT.md).
The orchestrator executed that allowance using the same nominated business name
and ordinary homepage, without substituting the later supplied location URL,
prefilling geographic facts or issuing a separate source fetch.

Preserved branch `devin/sol-smart-consultant-intake-plan`, HEAD `2a21f85`.
All 316 reviewed product hashes matched both the shared checkout and the dedicated
local copy before and after execution. The four protected notes matched before
execution, and the index was empty. No product code, main integration, commit,
push or deployment was performed.

The dedicated runtime is `http://127.0.0.1:3044/audit`, using the repository's
default development bundler and existing development-mode rate behavior. It is
not a production Worker/rate-binding validation. Credentials were reused through
the existing environment loader in memory, without opening or printing their
values. Recorded configuration: live mode, direct OpenAI, `gpt-5.6-luna`, low
reasoning, USD 1.04786450 carryover floor, USD 5 ceiling, provider testing off.

## Observed execution

Started at 03:55:27.236 UTC (10:55:27.236 WIB). These are actual live observations,
separate from the preceding fictional driver check and independent offline tests.

| Measure | Result |
|---|---|
| Periksa actions | 1 |
| Identity / extraction API requests | 1 / 1, both HTTP 200, both live |
| Extraction provider attempts / retries | 1 / 0 |
| Returned model / hosted-search calls | `gpt-5.6-luna` / 1 |
| Homepage excerpt status | `included` |
| Observed summary readiness | 15.680 seconds after Periksa |
| Entry / post-entry typing | 38 / 0 characters |
| Business corrections | 0 |
| Confirmation | Enabled, never clicked; one remaining decision |
| Question / observation / report requests | 0 / 0 / 0 |
| Unexpected or blocked live requests | 0 |
| New accounted cost | USD 0.01454705, `provider_usage` basis |
| Cumulative accounted cost | USD 1.06241155, including historical carryover |
| Remaining ceiling headroom | USD 3.93758845; no permission for another attempt |

The transport counts remain one identity and one extraction after both viewport
inspections and source disclosure. The browser guard was separately checked
before the live run with intercepted fictional responses: one identity, one
extraction, and a deliberately blocked downstream request, with zero real
provider work. That fictional blocked request is not a live incident.

## Prepared meaning and presentation

- Whole-brand focus remained the unchanged Nuave suggestion.
- Category and four offering proposals were populated. Two service channels
  were selected: at the business and delivery. Online service use was unselected.
- Reach was `seluruh` / **Seluruh Indonesia**, with no active city list, sourced
  as **Dari website Anda**. No required reach/area gap blocked confirmation.
- Optional target customer remained blank. No input was added to improve
  apparent completeness or change a proposal to an owner-supplied fact.
- The model's reach evidence cited the official outlets page and its published
  outlet/city counts. The expanded source disclosure showed the homepage plus
  three official source paths, including the location directory.
- Desktop 1440 × 1000 and mobile 390 × 844 screenshots of the same tab show the
  same selected meanings, visible origins, expanded sources and primary action.
  Mobile controls and source URLs remain readable through vertical scrolling.
  Long optional text remains in single-line fields, with only part visible at
  once; the audit made no edit or claim to test every correction interaction.

**Interpretation:** the specific missing-required-reach symptom did not recur.
The prepared national proposal is consistent with the accepted broad-presence
meaning and the previously observed distributed official network. The model
returned the relevant location URL; one hosted-search call is recorded. This
does not independently prove which page text the provider retrieved or every
step of its interpretation. The evidence note itself gives counts, not a full
geographic explanation. No source was refetched to resolve that observability gap.

No explicit universal-delivery statement was rendered, but the UI/context still
keeps broad reach and channel values separate rather than encoding delivery
boundaries per channel. No question/report call tested later model inference.
Generic English remains within a proposed optional differentiator; this is
recorded against the existing R-03 limitation, not a new founder rejection.
Individual offering accuracy is not independently established by field presence.

## Execution limits and evidence

The first temporary startup used explicit Webpack and failed on `node:crypto`
before any live API request. Switching only the temporary launcher to default
`next dev` resolved startup; product files were unchanged. The fictional guard
check then passed before the single live click.

After desktop/mobile screenshots were successfully saved, supplementary text
harvesting timed out because the temporary driver looked for a nonexistent
`main` element. Both screenshots and the post-inspection request state survived.
A subsequent Computer Use attempt stalled awaiting OS permissions and supplied
no additional UI evidence. No extra preparation was performed. The live tab is
preserved, source disclosure expanded, confirmation unclicked and API guard
disarmed for further preparation; downstream routes remain blocked in that tab.

Restricted evidence directory:
`/var/folders/6t/nwjsmszx33z6xbybswmlv0hr0000gp/T/nuave-location-live-zzkhqrdi/`.
It contains the dispatch claim, sanitized returned fields/cost telemetry,
configuration metadata, driver smoke result, screenshots, preservation manifest,
startup/capture limitations and artifact hashes. No raw provider payload, full
source HTML, HAR, complete browser storage or credential value was retained.

| Evidence | SHA-256 |
|---|---|
| `live-observation.json` | `a22d59eae38eaeedff71889b79310b1c184d01df31d83e0e7779eaec7c3452dc` |
| `desktop-sources.png` | `668e5163e4757f9d2a99c1963feb6baed36f5be8616a2f19281c16a34fd02c7d` |
| `mobile-sources.png` | `a7c46715927a90b84425067b684b65a296fc3b2cb85046573f0c6467c7c592c1` |

No broad gates were repeated because no product change was made. Product
preservation, accounting and documentation whitespace/link checks are the
closeout checks for this turn.

Repository files changed: `docs/DECISION_LOG.md`, `docs/NOW.md`, `docs/INDEX.md`,
`specs/README.md`, this result, `F03_LOCATION_SOURCE_WALKTHROUGH_PROMPT.md`,
`ACCEPTANCE_EVIDENCE.md` and `VERIFICATION.md`. Temporary launcher/driver and
screenshots are outside Git. No product file changed.

## Founder judgment and next step

After seeing desktop/mobile evidence, the founder answered **“Accept the reach
and prepared summary”** to whether national reach is appropriate and the
experience now feels like confirming a consultant's prepared understanding.
This is the requested acceptance of the observed proposal/experience, with no
correction supplied. It resolves the prior completeness objection for this case.
Do not repeat the founder question. The app remains unconfirmed; this reply did
not click its action or authorize question generation or another preparation.

Next is [independent acceptance closeout](./ACCEPTANCE_CLOSEOUT_REVIEW_PROMPT.md)
using the existing evidence and literal approved criteria. AC-01 specifies an
intercepted fictional rich-local case; assess the existing test evidence rather
than treating this whole-brand live case as a reason to require another paid
run. Retain the recorded language and observability limitations and assess their
materiality against actual requirements. Founder acceptance does not silently
rewrite the general language contract. F-01 stays closed; F-03/AC-07 await formal
closeout; Spec 011 stays Approved, not Verified. Current-main reconciliation
remains separate before PR readiness. Do not rerun this consumed allowance.
