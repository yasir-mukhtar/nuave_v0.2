# F-03 location-source correction: preparation-only founder walkthrough

**Completed — one preparation-only walkthrough; allowance consumed, 2026-09-24.**
Prepared by the orchestrator on 2026-09-24 after accepting the independent
offline PASS. The founder replied **“Approved”** to this bounded live walkthrough,
including its eligible technical retry, carryover floor and USD 5 total ceiling.
The [execution result](./F03_LOCATION_SOURCE_WALKTHROUGH_RESULT.md) records one
Periksa, one successful extraction, no retry and an enabled, unclicked confirmation.
The founder subsequently accepted the reach and prepared summary. This handoff is now historical execution authority;
do not execute it again. All earlier consumed allowances remain untouched.

Repository: `/Users/hy4-mac-006/nuave_v0.2`.
Role: acceptance worker. Objective: use one ordinary preparation to establish
whether the corrected extractor produces a supported, useful whole-brand reach
proposal, then present the unchanged result for founder judgment. No code task.

## Required context and preserved baseline

Read `AGENTS.md`, `README.md`, `docs/NOW.md`, `docs/WORKFLOW.md`; Spec 011's
Experience, R-01 through R-05, R-13/R-14, R-21/R-25/R-27 and AC-07; then
`F03_LOCATION_SOURCE_PROPOSAL.md`, `F03_LOCATION_SOURCE_REVIEW.md` and this
handoff. Use the previous walkthrough's **Walkthrough and evidence** section
for evidence conventions only; its allowance is consumed and must not rerun.

Preserve branch `devin/sol-smart-consultant-intake-plan`, HEAD `2a21f85` and all
existing edits. Founder approval to finish on this baseline is settled. Do not
integrate main, repeat broad gates, change runtime/tests, reset, stash, switch,
stage, commit, push or deploy. Check the full 316-file reviewed product against
`/private/tmp/nuave-f03-location-j1557mch/candidate-product-hashes.json` and the
four protected-note hashes from the diagnostic handoff's hash table only. Stop
on unexplained product changes; do not execute the old diagnostic or inspect
its private evidence. Preserve existing browser sessions and historical records.

## Input and proposed allowance

Use the same founder-nominated exact business name and homepage from
`/private/tmp/nuave-f03-walkthrough-plan-aj_sffr1/nominated-input.json` (public
input fields only, not its obsolete execution flags). If unavailable, recover
the exact nomination from conversation; never guess a replacement. Retain the
ordinary whole-brand default. Do not substitute the subsequently supplied
outlets URL or inject its contents: this test must exercise ordinary discovery
from the homepage input. Do not select a second business.

Founder approval of this handoff authorizes only:

- One fresh local preparation session on a dedicated loopback instance serving
  the reviewed product; check and record the actual URL and mode before entry.
  Use one fresh browser tab at desktop size, then mobile viewport emulation in
  that same tab. Existing sessions remain untouched.
- One `Periksa`: existing identity/head/icon work, the single bounded server
  document read with existing safe redirects, and one extraction API request.
  No separate source prefetch or added application request.
- Direct OpenAI extraction with the approved model/settings and hosted search.
  At most two paid attempts: initial extraction plus its existing automatic
  technical retry only when eligible. Valid partial/empty output never retries.
  No second click, manual retry, reload to restart or replacement attempt after
  uncertain dispatch. Hosted tool/page limits remain advisory and actual usage
  must be accounted honestly.
- USD 5 total ceiling with **USD 1.04786450 minimum historical carryover**,
  retaining any higher configured total or additional known spend. This includes
  the accepted historical estimate, not independently verified billing. Do not
  reset or double-count old spend. A new empty preparation ledger is permitted
  only for this fresh allowance; retain old exhausted ledgers and execution locks.
- Reuse configured credentials without opening, copying or printing secrets.
  Establish numeric carryover, live mode and model configuration before the
  click. Unknown accounting/configuration is a blocker, not a zero-cost default.

Zero live confirmation, question-generation, observation or report requests.
Do not press `Sudah sesuai — buat pertanyaan audit`, including keyboard submit.
No production change, main integration or publication is authorized.

## Execute once and inspect

1. Establish product/hash/configuration preflight and request counting. Block
   question/run/report routes in the acceptance browser where tooling permits.
   Do not retain request bodies or a HAR. Any temporary browser driver stays
   outside the repository and is checked with intercepted fictional transports
   before the live click; no new diagnostic runtime or provider wrapper.
2. Enter the exact nominated inputs and click once. Record readiness time,
   identity/extraction API requests, model attempts/retries, excerpt status and
   sanitized actual/reserved costs. Let the sequence reach its summary or
   terminal error. Do not infer server fetch counts from browser API counts.
3. Before editing any business value, record reach, active areas, channels,
   category, offerings, optional customer context, origins, required gaps and
   whether confirmation is available. Inspect the existing source disclosure
   and actual evidence URLs on desktop and mobile in the same prepared session.
   Record explanatory-language issues separately from unchanged official names.
4. Distinguish a returned source link from demonstrated retrieval/use. Record
   only what existing safe telemetry/disclosure establishes; do not invent an
   independent receipt or refetch pages to fill an observability gap. A national
   proposal must have relevant current network evidence, no required city list
   and no universal-delivery claim. International reach needs explicit current
   Indonesia-and-abroad support. Never force the expected enum or fill missing
   values to make the test pass. Optional customer absence is acceptable.
5. Present the prepared summary and source disclosure for founder judgment.
   Ask whether the proposed reach is appropriate and whether the experience now
   feels like confirming a consultant's prepared understanding. Record typing,
   substantive choices and any requested correction separately from the initial
   result. Count final confirmation as one remaining, unexecuted decision.
   Preserve the tab and stop before that action even if it becomes enabled.

If discovery or completeness still fails, retain that result and identify the
observed failing layer and unknowns. No prompt tuning, model rerun or additional
source access is included. Previous national interpretation is an expectation,
not a prefilled fact or a required answer regardless of newly returned evidence.

## Evidence, report and limits

Keep business-identifying screenshots/minimal source observations outside Git
in a new restricted temporary directory. No raw provider payloads, unnecessary
personal information or credentials in evidence or logs. Use sanitized field
presence, counts, origins and failure classifications in repository records.

Write `F03_LOCATION_SOURCE_WALKTHROUGH_RESULT.md` in this spec package and append
actual evidence to `ACCEPTANCE_EVIDENCE.md` and `VERIFICATION.md`; update
`docs/NOW.md` accurately. Separate observed output, supported interpretation,
founder feedback and unknowns. Report exact allowance consumption, cumulative
accounting, files changed, product preservation and `git diff --check`.
If founder feedback is pending, say so; do not manufacture acceptance or repeat
preparation while waiting. Do not dispatch another agent.

Successful preparation is not automatic closure of Spec 011. This whole-brand
chain case does not establish the representative rich-local criterion. Existing
language, source-disclosure and other acceptance limits need an explicit evidence
review; the founder's earlier acceptance of other evaluated aspects is preserved.
After this walkthrough, the orchestrator reviews remaining acceptance criteria
and any narrow follow-up or explicit exception. Current-main report conflicts
remain separate work before PR readiness. F-01 stays closed; F-03/AC-07 stay open
until acceptance is explicitly resolved; Spec 011 remains Approved, not Verified.
