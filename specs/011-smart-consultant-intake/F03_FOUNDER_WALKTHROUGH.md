# F-03: founder preparation walkthrough

Prepared and executed: 2026-09-23. **One authorized Periksa completed; allowance
consumed.** The founder authorized its permitted technical retry, USD 5 ceiling
and USD 1.03498655 minimum carryover, stopping before final confirmation.
[Execution evidence](./ACCEPTANCE_EVIDENCE.md#2026-09-23-refreshed-live-preparation-executed)
records one provider attempt, no retry, desktop/mobile inspection and the disabled,
unclicked confirmation. The founder's 2026-09-24 review finds completeness
lacking and everything else acceptable. Source disclosure inspection and
independent acceptance review remain incomplete. Preserve this plan as the
execution boundary; do not rerun it. The consumed earlier diagnostic remains untouched.

The [recorded founder review](./ACCEPTANCE_EVIDENCE.md#2026-09-24-founder-review-received)
focuses the next work on completeness. Empty target customer is optional; empty
market reach is required and still blocks confirmation. No business value was
supplied or changed by this review, and no further live work was authorized.

## Objective and starting evidence

Use one ordinary `Periksa` action to let the founder judge the corrected
prepared summary on desktop and mobile in the same tab. Stop before final
confirmation or question generation. Record the result in
`ACCEPTANCE_EVIDENCE.md`, separating observations from founder judgment.

The [independent re-review](./F03_PRODUCT_CORRECTION_REVIEW_2.md) passes both
selector corrections, seven original assertions, 233 focused tests,
`validate:fast` and `verify`: 1,232 tests, both builds and 31 browser checks in a
matching isolated copy. Shared-tree lint remains obstructed. No code revision
or repeated broad gate is needed for this documentation/acceptance task.
F-01 is closed; F-03/AC-07 remain open; Spec 011 remains Approved, not Verified.

Repository: `/Users/hy4-mac-006/nuave_v0.2`.
Branch: `devin/sol-smart-consultant-intake-plan`.
HEAD: `2a21f856d33264887df6287f9b6d9dd22468fea5`.
The reviewed implementation includes uncommitted/untracked files; HEAD alone
does not identify it. Preserve the working tree, original reviews, protected
notes, old records and consumed diagnostic allowance. Do not reset, stash,
switch, stage, commit, publish or deploy.

Read `AGENTS.md`, `README.md`, `docs/NOW.md`, `docs/WORKFLOW.md`, then Spec 011's
Experience, R-21/R-25/R-27, AC-07 and failure-classification sections,
[approved correction scope](./F03_PRODUCT_CORRECTION_SCOPE.md), and the
re-review above. Use this handoff for the refreshed walkthrough; do not execute
the old acceptance/PDF or diagnostic prompts.

## Source and required meanings

The founder supplied the exact business name and public homepage in the
2026-09-23 reply to the source-nomination question. Use those exact inputs;
an owner-only copy for this walkthrough is at
`/private/tmp/nuave-f03-walkthrough-plan-aj_sffr1/nominated-input.json`.
That file contains only the current public business input and nomination
reference; it is permitted input for this task, not old private audit evidence.
If unavailable, recover the founder's nomination message without guessing.
Keep business-identifying screenshots and unnecessary source content out of Git.

At planning time no source page had been fetched. No branch or city was supplied: retain the
whole-brand default and the supplied homepage; do not substitute a branch,
location or another page. Offerings, service channels and geographic reach
remain unverified. The selected input is not automatically the representative
rich local case, and its nomination does not waive that acceptance criterion.

| Meaning | Before execution | What the walkthrough must establish |
|---|---|---|
| Identity/source | Exact name and homepage supplied by the founder | Typed identity remains owner-supplied; record discovered identity/source separately. |
| Audit focus | Existing default is whole brand | `Brand secara keseluruhan` remains `Saran Nuave` and unconfirmed. |
| Category and offerings | Unknown for the nominated page | Record returned proposals, visible selections and any founder correction; do not equate populated values with verification. |
| Service channel and reach | Unknown | Retain empty values unless supported; record any founder choices and their owner origin. |
| Local area | Unknown; required if local/multiple-area reach is selected | A contact address alone is not coverage. Record the supplied area or the remaining required gap. |
| Optional meanings | May remain absent | No filling or retry merely to make the summary look complete. |

The earlier diagnostic populated category and eight offering proposals but
left channels/reach/areas empty; its passage did not establish every offering.
It is not evidence of a complete rich local summary. Reusing that business
requires a new nomination and honest unknowns. If required customer choices
or typing exceed the rich-case target, record that limit; do not invent facts
or silently waive acceptance. Do not select a second business or rerun to get
a better result under this allowance.

## Authorized execution boundary (now consumed)

- One new preparation session on a dedicated loopback instance at
  `http://127.0.0.1:3034/audit` (proposed and subsequently used endpoint).
  Availability and actual configuration were checked before enabling live work.
  Keep existing sessions untouched. Use a fresh acceptance browser context,
  with one tab at 1440 × 1000, then 390 × 844 through viewport emulation.
- One `Periksa` action: one identity request with its existing head/icon
  behavior, then one extraction request including the new bounded document
  read and permitted safe redirects. No separate source prefetch or extra page.
- Protected direct OpenAI extraction, `gpt-5.6-luna`, low reasoning and existing
  hosted search/settings. At most two paid extraction attempts: the initial
  attempt plus the existing automatic technical retry, only when applicable.
  An empty/partial valid draft does not permit another attempt. A second click,
  manual retry or restart after uncertain dispatch needs a new allowance.
- Preserve the USD 5 ceiling and historical accounting. The last recorded
  total is USD **1.03498655**, comprising an explicitly estimated USD 1.00
  reserve plus USD 0.0217347 and USD 0.01325185 accounted extraction costs.
  This is not verified billing. The founder authorized that total as the minimum
  server carryover floor for this fresh session, retaining any higher
  configured total or additional known spend. Do not count those same old
  calls twice. A new empty preparation ledger is allowed only as the explicitly
  new allowance; the old exhausted ledger and diagnostic lock remain unchanged.
- Reuse configured credentials without opening, copying or printing secrets.
  Establish the numeric carryover and live/synthetic mode before the click;
  unknown spend/configuration is a blocker, never zero. The later explicit
  authorization permitted the dedicated local runtime used for this execution.
- Zero confirmation, question, observation or report actions/requests. No PDF
  rerun, historical reactivation, production change or publication.

Suggested authorization for the nominated input and stated carryover floor:

> I authorize this one new preparation-only walkthrough for the business and
> exact public URL I nominate: one Periksa action, existing identity/icon work,
> the bounded document read, and one extraction with at most its existing
> automatic technical retry. Preserve the stated historical accounting and
> current limits. Use one tab for desktop/mobile and stop before final
> confirmation; no question generation, observations or report calls.

The founder has now granted this new allowance in conversation; it does not
reuse the diagnostic. The paragraph above preserves the proposed scope. Spec 011
AC-07 requires **“After separate authorization for one live preparation”**;
the [2026-09-23 decision](../../docs/DECISION_LOG.md#2026-09-23--approve-the-f-03-product-correction)
authorized code/offline checks only. The later explicit founder message supplies
the separate live permission; it does not authorize final confirmation or
downstream provider work.

## Walkthrough and evidence

1. Verify the reviewed product hashes, branch/HEAD, empty index and four
   protected-note hashes without opening old private evidence. Establish the
   dedicated runtime and fresh tab. Arm request counting before any entry:
   allow the one identity/extraction sequence; keep question/run/report routes
   blocked where browser tooling permits. Do not retain request bodies or a
   HAR. If a temporary browser driver is needed, keep it outside the repo and
   check it with fully intercepted fictional responses before live execution.
2. Enter the authorized name/URL; count characters. Click `Periksa` once and
   measure until the summary or an intake-local terminal error appears. Keep
   the allowance armed through the asynchronous identity/extraction sequence;
   a site-wide alert must not terminate it prematurely as in the old driver
   incident. Record attempted, blocked and completed API requests separately.
3. Record `source_excerpt_status`, preparation mode, returned field presence,
   selected meanings/origins, required gaps, provider attempt/retry counts and
   sanitized accounted costs/cost basis. Distinguish the one extraction API
   request from its paid attempts. Source status can establish the completed
   document stage; actual server HTTP/redirect counts are unknown unless
   directly observed. Do not infer all source transports from browser counts
   or claim to observe the model's actual use of the excerpt.
4. Let the founder inspect the desktop summary and source disclosure. Record
   typing, material decisions and corrections after `Periksa`, before helping
   or changing anything. Owner choices remain owner-sourced. Change only the
   viewport for mobile; inspect readability, controls, scrolling, origins and
   primary-action visibility. Preserve the same prepared state. Do not repeat
   preparation or reopen a tab for mobile.
5. Stop before **`Sudah sesuai — buat pertanyaan audit`**, including keyboard
   submission. The final confirmation counts as one remaining decision, not
   an executed one. The rich-case target is zero post-entry typing, one summary
   and at most two substantive decisions including that unclicked confirmation.
   Viewport changes/disclosures do not count as business decisions. Ask the
   founder: **“Does this feel like confirming a consultant's prepared
   understanding rather than filling a form?”** Record the exact answer and
   whether the prepared meaning is accurate. No answer means judgment pending.
6. Stop on sensitive content, a source/privacy/rate error, exhausted limits,
   uncertain dispatch or an unexpected request; preserve only safe evidence.
   A blocked forbidden request is still a finding. For missing material facts,
   distinguish source absence/access, extraction, mapping and presentation
   only when supported; otherwise record unresolved. No extra diagnostic call.

Append sanitized outcomes to `ACCEPTANCE_EVIDENCE.md` and an attributed status
to `VERIFICATION.md`; update `docs/NOW.md` only for changed facts/next action.
Include authorization reference, date, product state, viewports, actions,
timing, counts/costs, source outcome, corrections, zero-downstream evidence,
founder judgment and remaining limits. Any necessary screenshots stay in a
new owner-only temporary directory outside Git; retain no raw provider response,
full HTML/excerpt, credentials or complete browser storage. Sensitive material
must not be copied onward; restrict it and notify the founder.

Return the evidence path, actual results and remaining gate for independent
review via the founder. Do not dispatch agents or change code during acceptance.
A successful walkthrough and founder judgment can support closing F-03/AC-07;
this plan and the offline PASS cannot mark Spec 011 Verified.
