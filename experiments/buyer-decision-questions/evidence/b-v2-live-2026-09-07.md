# B v2 live generation record — 2026-09-07

This is an addendum to the pre-run proposal. It records observed provider
output; it is not a quality verdict and does not replace founder review.

## Invocation

The founder explicitly authorized this generation. The isolated runner was:

    EXPERIMENT_BDQ_LIVE=1 \
    OPENAI_AUDIT_MODEL=gpt-5.6-luna \
    OPENAI_AUDIT_REASONING_EFFORT=low \
    NUAVE_QUESTION_PROVIDER=opencodego \
    BDQ_INSTRUCTION_ID=condition-b-v2 \
    npx vitest run --config experiments/buyer-decision-questions/vitest.live.config.mts b-v2

- Provider: existing OpenCode Go Responses API
- Requested model: `gpt-5.6-luna`
- Web search: disabled
- Calls scheduled: 3 (one B call per frozen fixture)
- Calls completed: 3
- Retries/repairs: none
- Condition A calls: none
- Terra calls: none
- Credentials: existing `OPENCODEGO_API_KEY` mechanism; value was not printed

The live test process exited non-zero because its assertion correctly rejects
invalid B contract output. The provider calls themselves returned completed
records, which remain immutable under `results/`.

## Records and mechanical result

| Fixture | Run ID | Provider status | B contract | Mechanical issue |
|---|---|---:|---:|---|
| Kopi Sudut (development) | `initial-20260907-060224-5f94e5` | completed | valid | none |
| Kopi Bukit Biru (held-out) | `initial-20260907-060232-9a65cf` | completed | invalid | 5 named requests instead of 4 |
| Laundry Segar Kilat (held-out) | `initial-20260907-060239-85003b` | completed | invalid | only 1 `compare_alternatives`; no unnamed comparison |

All three records show requested and returned `gpt-5.6-luna`, one HTTP call,
no provenance errors, and `source: model`. The invalid records retain their
raw model output and expose `validation_issues`; no old A fallback or repair
was applied.

## Interpretation boundary

This is evidence that the v2 instruction produced more direct-looking shapes
in some places, but it is **not** evidence that v2 passes the founder's
language bar. The founder must review the paired B v1/B v2 bundle. The existing
6/4 and coverage rules remain unchanged; do not lower them because the v2
output failed them.

Review bundle:

    experiments/buyer-decision-questions/review/index.html?bundle=data-v2.js

The bundle pairs the corresponding B v1 and B v2 records while hiding
confirmation/challenger packs. It is a full ten-question-per-pack review;
founder marks and pass-2 checks remain incomplete until the founder performs
them.
